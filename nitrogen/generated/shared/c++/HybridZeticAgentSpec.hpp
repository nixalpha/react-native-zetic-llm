///
/// HybridZeticAgentSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `NativeLoadModelConfig` to properly resolve imports.
namespace margelo::nitro::zeticllm { struct NativeLoadModelConfig; }
// Forward declaration of `AgentOptions` to properly resolve imports.
namespace margelo::nitro::zeticllm { struct AgentOptions; }
// Forward declaration of `AgentStateSnapshot` to properly resolve imports.
namespace margelo::nitro::zeticllm { struct AgentStateSnapshot; }
// Forward declaration of `AgentEvent` to properly resolve imports.
namespace margelo::nitro::zeticllm { struct AgentEvent; }

#include <NitroModules/Promise.hpp>
#include "NativeLoadModelConfig.hpp"
#include <functional>
#include <optional>
#include <string>
#include "AgentOptions.hpp"
#include "AgentStateSnapshot.hpp"
#include "AgentEvent.hpp"

namespace margelo::nitro::zeticllm {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `ZeticAgent`
   * Inherit this class to create instances of `HybridZeticAgentSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridZeticAgent: public HybridZeticAgentSpec {
   * public:
   *   HybridZeticAgent(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridZeticAgentSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridZeticAgentSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridZeticAgentSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<void>> loadModel(const NativeLoadModelConfig& config, const std::optional<std::function<void(double /* progress */)>>& onDownload) = 0;
      virtual std::shared_ptr<Promise<void>> start(const std::string& task, const std::optional<AgentOptions>& options) = 0;
      virtual void pause() = 0;
      virtual void resume() = 0;
      virtual void stop() = 0;
      virtual void sendInstruction(const std::string& text) = 0;
      virtual std::shared_ptr<Promise<std::string>> snapshot() = 0;
      virtual AgentStateSnapshot getState() = 0;
      virtual void onEvent(const std::function<void(const AgentEvent& /* event */)>& callback) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "ZeticAgent";
  };

} // namespace margelo::nitro::zeticllm
